# Claude Code handoff: list-page consistency

Copy the prompt below into Claude Code in the cd_capacity repository after the 0.11.1 release is available.

---

Complete the Product UI 0.11.1 adoption across Projects, Retainers and Clients. This is a coordinated page-consistency migration, not only a dependency update.

Read the installed package's README, docs/release-0.11.md, docs/list-pages.md, docs/data-table.md and docs/ai-guidance.md before editing. The versioned reference is https://github.com/tomrosscd/cd-product-ui/blob/v0.11.1/docs/list-pages.md. The executable synthetic example is docs/list-page.stories.tsx at the same tag; the hosted catalogue has Patterns / List pages / Projects, Retainers, Clients and Narrow. The hosted catalogue follows main, so use the tag for exact-version behaviour.

1. Inspect the repository instructions, current branch, package manager, manifest, authoritative lockfile, installed Product UI version and vendor archive. Inventory the table, filters, page actions, table options and local styling for all three routes before changing them. Preserve unrelated work.
2. Download the immutable archive and SHA256SUMS from https://github.com/tomrosscd/cd-product-ui/releases/tag/v0.11.1, verify the checksum, install the exact archive and update the authoritative lockfile. Preserve the established vendor workflow. Do not substitute a branch checkout or overwrite an old versioned archive.
3. Create or reuse one application-owned list-page wrapper using Stack, PageHeader, FilterToolbar and DataTable as specified in docs/list-pages.md. Apply it to all three routes. New/Import/reminder actions belong in PageHeader, with the same small size. Views/Configure belong in FilterToolbar.actions. Use the shared bounded search layout and labelled filters; remove superseded local workarounds after verifying the result. Preserve the already-correct compact sidebar.
4. Migrate RetainerTable's hand-built table, native filters, custom primary/import links and local presentation onto the shared components. Preserve all columns, permission checks, retainer types, status labels, inactive filtering, calculations, remaining-hours thresholds, rounding, missing values, dates and navigation. Do not change business rules or drop columns to make the layout fit. Use horizontal table scrolling where required.
5. Align Clients and Projects with the same composition, compact table density, full pagination and 15-row initial page size. Preserve persisted user preferences when they exist. Move New client into PageHeader. Use a non-empty `all` sentinel for StyledSelect's All statuses option and translate it at the existing filter/URL boundary. Keep query parameters, saved views, project filters and inline editing intact.
6. Move conditional Close warranty into the existing row Actions menu, preserving its permissions, confirmation and handler. Prevent project status markers from wrapping onto their own line. Do not invent unsupported ActionMenu props. Document any real library gap with a reproduction instead of silently adding another button style.
7. Run the application's frozen install, type checks, tests and production build. Add focused regression coverage for preserved filtering and permissions where migration changes their wiring. In a browser, compare all three routes at 1440px and 390px using representative data, long names, no matches, selected filters, saved views, menus and pagination. Measure mixed small button/link heights (32px desktop, 48px mobile), search width (at most 320px on desktop), toolbar alignment and document overflow. Verify keyboard focus and menu dismissal. Include before/after screenshots in the review.
8. Update the app's coding guidance with a link to docs/list-pages.md and the shared wrapper location. Report each route's final component mapping, remaining local exceptions, installed archive checksum, tested commit and exact validation results. Distinguish synthetic checks from authenticated app checks. If browser access is unavailable, leave visual verification explicitly pending rather than claiming the migration is complete.

Keep the existing data access, authentication, API requests and persistence. Do not run destructive business actions, send reminders/import data for visual testing, or change the shared library's source inside node_modules. Prepare one focused application PR with the dependency and migration changes together; follow the application's review/deployment policy.
