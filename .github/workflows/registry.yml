# Hosting was retired in favour of release archives. Retain the local pilot and drift checks.
name: Registry hosting retired
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Registry hosting is retired. Install the GitHub Release archive. See README.md.'
